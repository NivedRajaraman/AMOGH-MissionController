
#ifndef _TYPEDEF
#define _TYPEDEF

#include <map>
#include <string>
#include <iostream>
#include "stdio.h"

#define PIXERR 1			// pixel error counted as 0 error (target reached)
#define ANGERR 1			// angle error counted as 0 error (target reached)

#endif
